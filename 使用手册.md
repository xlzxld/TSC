# 使用手册（大白话版）

> 适用版本：v3.0.0 | 本手册是给人看的说明书，**不属于部署单元**——部署项目时不用拷它，它也不占 AI 的上下文。

---

## 一、这是个什么东西

一句话：**一套让 AI 编码助手在你项目里守规矩的说明书，外加一个能跑的门禁。**

它管两件事，管法完全不同：

1. **管 AI 的日常行为**——改完代码必须贴证据、删东西之前必须先问你、没验证过不许说"已完成"。靠的是 `AGENTS.md` 这份提示词，把违规概率压低（但不是归零）。
2. **管仓库的硬底线**——密钥不许进库、提交信息必须规范、主干不许直推。靠的是**可选**的 `enforcement/` 钩子与 CI，装了才生效，跟 AI 自觉不自觉没关系。

## 二、落地后项目里有什么

只有根目录两样东西：

| 位置 | 一句话说明 | AI 什么时候读它 |
|---|---|---|
| `AGENTS.md`（根目录） | 规矩本体，77 行 | **常驻**——AI 一进项目就自动读（主流工具都认根目录这个文件名） |
| `.agents/tsc.py` | 唯一核心脚本：接入 / 同步 / 门禁 | 你敲命令时 |
| `.agents/project.py` | 你自己项目的门禁命令 | 跑门禁时读 |
| `.agents/AUDIT-SPEC.md` | 体检的具体做法 | 你说"体检"时才读一次 |
| `.agents/BOOTSTRAP.md` | 适配的流程 | 你说"适配"时才读一次 |
| `.agents/enforcement/` | 可选机械保险丝 | **默认不启用**，需要时照其 README 拷出去 |
| `.agents/test/` | 给契约本身做体检的题库 | 平时完全不用 |

记住核心就够了：**平时占 AI 脑子的只有根目录 `AGENTS.md` 一个文件。**

> v3.0.0 相比 v2 的变化：以前要把"3 个文档 + 1 个目录"散在项目根目录，现在全收进 `.agents/`，根目录只留 `AGENTS.md`。`sync` 会自动把旧结构搬进去，**不会自动删文件**。

## 三、先装技能（一次，三平台通用）

这个仓库本身就是技能，名字叫 `tsc`（根目录有 `SKILL.md`）。把仓库放进对应平台的技能目录：

| 平台 | 放哪里 |
|---|---|
| macOS / Linux 的 Claude Code | `~/.claude/skills/tsc/` |
| WorkBuddy | `~/.workbuddy/skills/tsc/` |
| Windows 的 Claude Code | `%USERPROFILE%\.claude\skills\tsc\` |

更新技能 = 在技能目录里 `git pull`。**没有打包、没有构建步骤。**

## 四、接入一个项目（每个项目一次）

在项目根目录跑一条命令：

```bash
python3 "<技能目录>/.agents/tsc.py" install --from "<技能目录>" --project .
```

先加 `--dry-run` 可以只看将改动什么、不写盘。跑完后项目根目录只多两样：`AGENTS.md` 和 `.agents/`。

**接完必须做一件事**：填 `.agents/project.py`，改成你项目真实的 fmt / lint / test / build 命令（没有的写 `None`），并把同样的取值填进 `AGENTS.md` 的 §2 表格，然后：

```bash
python3 .agents/tsc.py check-config
```

两边不一致会报错——这是故意的，防止 §2 表格和实际门禁各说一套。

> 提醒：`project.py` 里四条命令若全是 `None`，门禁会全程 skip，然后给你一个"全绿"的假结论。

## 五、日常怎么用（记三个词 + 三条命令）

对 AI 说：

| 你说 | 会发生什么 |
|---|---|
| **体检** | AI 全仓只读扫描（保证不改任何文件），给你一张 P0~P3 问题表，然后停下等你点单 |
| **修复 A-01** | 按表修问题：先跑基线 → 最小改动 → 带回归测试 → 门禁 → 提交；要删东西、装依赖会先停下来等你确认 |
| **适配** | 重算 §2，或做版本升级（重复说没有副作用，不会覆盖你的定制） |

自己在终端敲：

```bash
python3 .agents/tsc.py verify                      # 门禁：全绿才算完
python3 .agents/tsc.py sync --from "<技能目录>"     # 上游更新后同步，你的 §2 不会被覆盖
python3 .agents/tsc.py status                      # 看状态：接没接入、什么版本、§2 填好没
```

Windows 上把 `python3` 写成 `python`；不想记这个差异就敲 `.agents\verify.ps1`（Windows）或 `.agents/verify.sh`（macOS）。

日常循环长这样：

```
你提需求 → AI 改代码 → AI 汇报（必须带命令 + 退出码）
→ python3 .agents/tsc.py verify 全绿 → 提 PR
→ 你在 review 时抽查两件事：证据贴了没？豁免登记了没？
```

### 验收 AI 汇报的 5 个信号

| 看到 | 说明 | 怎么办 |
|---|---|---|
| 说"搞定了"但没贴命令和退出码 | 违反最核心的铁律 | 打回，不用商量 |
| 删文件 / 装依赖 / force push 之前没停下来问你 | 🟡 红线失守 | 打回 |
| 说"应该没问题""大概可以" | 开始滑坡 | 让它改成 ✅/❌ 二选一 |
| 改了你没让它改的邻近代码 | 顺手修改，污染 diff | 打回 |
| 用了豁免却没登记（要说"本次豁免 R-x.y" + 理由 + 回滚方式） | 豁免机制漏了 | 让它补登记 |

## 六、怎么确认它真的有用（两种测法）

**测提示词层**（AI 守不守得住）：新开一个会话，把 `.agents/test/EVAL-SET.md` 里的 7 句对抗话术逐条发给 AI，看它顶不顶得住。换模型、模型升级、改过契约之后各跑一次，结果记进 EVAL-SET 的回放表。

**测机械层**（钩子拦不拦得住）：造反例——

1. 提交一个假密钥 → gitleaks 必须拦下（需先启用本地钩子层或 CI）；
2. 提交一条 `update stuff` 这种烂信息 → commitlint 必须拦下（同上）；
3. 故意改坏一个文件跑 `python3 .agents/tsc.py verify` → 必须红；
4. 直接 push 主干 → 分支保护必须拒（完整档）。

任何一个没拦住 = 配置有问题，照 `.agents/enforcement/README.md` 检查。

## 七、维护（很少发生，发生了照做就行）

| 情况 | 做法 |
|---|---|
| 换了测试框架 / lint 工具 | 改 `.agents/project.py` + `AGENTS.md` §2，跑 `check-config` 确认两边一致 |
| 契约出了新版本 | 跑 `python3 .agents/tsc.py sync --from "<技能目录>"`；§2 与 `project.py` 不会被覆盖 |
| 想改规矩本身 | 走 PR：改内容 + 同步 `VERSION` 与三处版本头 + CHANGELOG 记一笔 + 跑 `verify` 与 `wc -l AGENTS.md` |
| 项目里有历史遗留问题（老 lint 警告、一直跳过的测试） | 登记"已知豁免清单"：只管新增，不管存量。每条按 `命令:条目描述` 格式写，方便日后核对 |

## 八、常见问题

**Q：AI 根本不读 AGENTS.md 怎么办？**
主流工具（Claude Code、Cursor 等）会自动认项目根目录的 `AGENTS.md`。如果你用的工具不认，把文件内容贴进它自己的规则配置里（如 `.cursorrules`）。

**Q：项目里已经有 CLAUDE.md / 其他规则文件了？**
别留两份，人工合并成一份。两份契约并存会打架，AI 不知道听谁的。

**Q：一定要装执法包吗？**
不一定，默认就是不装。但要知道代价：不装机械层，AI 说的每次"已完成"都得你人肉复核。装了钩子，"门禁绿了"就可以基本放心。另外现在本机 **没有 `make`**，所以门禁统一走 Python，不再依赖 make。

**Q：为什么死守 80 行不让加规矩？**
规矩越多，AI 越记不住、越不生效。这是这套设计的第一原则：常驻提示词必须轻，机器能管的交给钩子，不占提示词预算。

**Q：契约以后还会改吗？**
已冻结。今后只有三种情况才动它：① 对抗回放出现失守；② 真实使用中因为条款有歧义出了事故；③ 两个以上不同来源独立发现同一个实质缺陷。其余一律只登记不动。想加固？加钩子，别加行数。
