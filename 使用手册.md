# 使用手册（大白话版）

> 适用版本：v2.1.1 | 本手册是给人看的说明书，**不属于契约三件套**——部署项目时不用拷它，它也不占 AI 的上下文。

---

## 一、这是个什么东西

一句话：**一套让 AI 编码助手在你项目里守规矩的说明书，外加一道机械保险丝。**

它管两件事，管法完全不同：

1. **管 AI 的日常行为**——改完代码必须贴证据、删东西之前必须先问你、没验证过不许说"已完成"。靠的是 AGENTS.md 这份提示词，把违规概率压低（但不是归零）。
2. **管仓库的硬底线**——密钥不许进库、提交信息必须规范、主干不许直推。靠的是 enforcement/ 里的钩子，装了就 100% 生效，跟 AI 自觉不自决没关系。

## 二、文件都是干嘛的

| 文件 | 一句话说明 | AI 什么时候读它 |
|---|---|---|
| `AGENTS.md` | 规矩本体，就 75 行 | **常驻**——AI 一进项目就自动读（主流工具都认根目录这个文件名） |
| `AUDIT-SPEC.md` | 体检的具体做法 | 你说"体检"时才读一次 |
| `BOOTSTRAP.md` | 部署和适配的流程 | 你说"适配"时才读一次 |
| `enforcement/` | 机械保险丝 | **AI 不读它**，是给人和 git 用的；不安装就等于不存在 |
| `test/` | 给契约本身做体检的题库 | 平时完全不用 |

记住核心就够了：**平时占 AI 脑子的只有 AGENTS.md 一个文件。**

## 三、怎么装（三档，按项目大小选）

### 最小档：只放一个 AGENTS.md

适合：个人项目、玩具项目、AI 只是偶尔改几行、错了也好回滚的场景。

1. 把 `AGENTS.md` 一个文件拷到项目根目录；
2. 对 AI 说：**"适配 / 初始化规范"**；
3. AI 会自己探测你的项目（用什么测试命令、什么 lint 工具），把文件里的 §2 表格填好，给你看一份数据来源清单，你确认就行；
4. 完事。`enforcement/` 整个不用管。

两个注意：
- 最小档没拷 AUDIT-SPEC.md，所以"体检"用不了；哪天想用，把 `AUDIT-SPEC.md` 也拷过去就行。
- 日后工具链变了（比如加了 pytest），手动改 §2 那张表格就行，就几行。

### 标准档：三件套 + 两个钩子（推荐的正式用法）

适合：正在迭代的真实项目。

1. 把三个 md 文件 + `enforcement/` 整个目录拷到项目根目录；
2. 说"适配"，同上；
3. **人肉执行** `enforcement/README.md` 的四步安装（约 10 分钟）：
   - `pip install pre-commit && pre-commit install && pre-commit install --hook-type commit-msg`
   - `npm i -D @commitlint/cli @commitlint/config-conventional`
   - 按 §2 登记的内容填 Makefile 顶部的变量；登记"无"的项填 `skip`（**千万别留空**，留空会报错——这是故意的，防手滑漏配）
4. 装完立刻做一次冒烟测试（见第五节）。

### 完整档：再加 CI 门禁和分支保护

多人协作、生产代码再上。在标准档基础上，照 `enforcement/README.md` 第 4 步配 GitHub 的 branch protection 和 gate CI。这一档是唯一对"人"也生效的（同事绕过 AI 直推主干也会被拦）。

**怎么选**：判断标准就一条——AI 写的代码越多、错了越贵，就越往高档装。

## 四、日常怎么用（只需要记三个词）

| 你说 | 会发生什么 |
|---|---|
| **体检** | AI 全仓只读扫描（保证不改任何文件），给你一张 P0~P3 问题表，然后停下等你点单 |
| **修复 A-01** | 按表修问题：先跑基线 → 最小改动 → 带回归测试 → 门禁 → 提交；过程中要删东西、装依赖会先停下来等你确认 |
| **适配** | 部署，或工具链变了之后重算 §2（重复说没有副作用，不会覆盖你的定制） |

日常循环长这样：

```
你提需求 → AI 改代码 → AI 汇报（必须带命令 + 退出码）
→ make verify 全绿（标准档以上）→ 提 PR
→ 你在 review 时抽查两件事：证据贴了没？豁免登记了没？
```

### 验收 AI 汇报的 5 个信号

| 看到 | 说明 | 怎么办 |
|---|---|---|
| 说"搞定了"但没贴命令和退出码 | 违反最核心的铁律 | 打回，不用商量 |
| 删文件 / 装依赖 / force push 之前没停下来问你 | 🟡 红线失守 | 打回 |
| 说"应该没问题""大概可以" | 开始滑坡 | 让它改成 ✅/❌ 二选一 |
| 改了你没让它改的邻近代码 | 顺手修改，污染 diff | 打回 |
| 用了豁免却没登记（要说"本次豁免 R-x.y" + 理由 + 回滚方式） | 豁免机制漏了 | 让它补登记 |

## 五、怎么确认它真的有用（两种测法）

**测提示词层**（AI 守不守得住）：新开一个会话，把 `test/EVAL-SET.md` 里的 7 句对抗话术逐条发给 AI，看它顶不顶得住。换模型、模型升级、改过契约之后各跑一次，结果记进 EVAL-SET 的回放表。

**测机械层**（钩子拦不拦得住）：造反例——

1. 提交一个假密钥 → gitleaks 必须拦下；
2. 提交一条 `update stuff` 这种烂信息 → commitlint 必须拦下；
3. 故意改坏一个文件跑 `make verify` → 必须红；
4. 直接 push 主干 → 分支保护必须拒（完整档）。

任何一个没拦住 = 配置有问题，照 `enforcement/README.md` 检查安装。

## 六、维护（很少发生，发生了照做就行）

| 情况 | 做法 |
|---|---|
| 换了测试框架 / lint 工具 | 对 AI 说"适配"，它只重算 §2 表格，其他内容不碰 |
| 契约出了新版本 | 说"适配"，会整体升级但保留你项目 §2 里的定制，升级前给你看 diff 确认 |
| 想改规矩本身 | 走 PR，三个文件 + 执法包同批改，CHANGELOG 记一笔 |
| 项目里有历史遗留问题（老 lint 警告、一直跳过的测试） | 登记"已知豁免清单"：只管新增，不管存量。每条按 `命令:条目描述` 格式写，方便日后核对 |

## 七、常见问题

**Q：AI 根本不读 AGENTS.md 怎么办？**
主流工具（Claude Code、Cursor 等）会自动认项目根目录的 `AGENTS.md`。如果你用的工具不认，把文件内容贴进它自己的规则配置里（如 `.cursorrules`）。

**Q：项目里已经有 CLAUDE.md / 其他规则文件了？**
别留两份，人工合并成一份。两份契约并存会打架，AI 不知道听谁的。

**Q：一定要装执法包吗？**
不一定，最小档就不装。但要知道代价：不装机械层，AI 说的每次"已完成"都得你人肉复核。装了钩子，"门禁绿了"就可以基本放心。

**Q：为什么死守 80 行不让加规矩？**
规矩越多，AI 越记不住、越不生效。这是这套设计的第一原则：常驻提示词必须轻，机器能管的交给钩子，不占提示词预算。

**Q：契约以后还会改吗？**
已冻结。今后只有三种情况才动它：① 对抗回放出现失守；② 真实使用中因为条款有歧义出了事故；③ 两个以上不同来源独立发现同一个实质缺陷。其余一律只登记不动。想加固？加钩子，别加行数。
